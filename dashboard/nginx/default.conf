server {
    listen 80;
    server_name dashboard.jewelry.local.dev;

    root /usr/share/nginx/html;
    index index.html;

    # ── Reverse proxy: /api/ → WordPress REST API ──
    # Eliminates CORS and self-signed cert issues for browser fetch().
    location /api/ {
        proxy_pass         http://jewelry_wordpress/wp-json/;
        proxy_set_header   Host jewelry.local.dev;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto https;
        proxy_read_timeout 30s;
        proxy_buffering    off;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip
    gzip on;
    gzip_types text/css application/javascript application/json text/plain;
    gzip_min_length 256;
}
